# Loaded automatically by Vite/Vitest in test mode.
# Dummy values so src/lib/supabaseClient.js (createClient) doesn't throw
# "supabaseUrl is required" when App.jsx / AuthContext are imported in tests.
# Safe to commit — not real credentials.
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=test-anon-key
